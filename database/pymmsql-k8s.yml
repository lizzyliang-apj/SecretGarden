# Default values for esb-job.
# This is a YAML-formatted file.
# Declare variables to be passed into your templates.
replicaCount: 1
image:
  repository: matrixcomputer/python:latest
  tag: latest
  pullPolicy: Always
  command: ~

resources:
  limits:
    cpu: 500m
    memory: 512Mi
  requests:
    cpu: 10m
    memory: 200Mi

serviceAccount: ~

env: ~


config:
  path: /data/etc
  list:
    hris_ods.conf : |
        host: azrsqluat01-svr-hr.database.windows.net
        user: hrodsuat@azrsqluat01-svr-hr
        password: dq*3CE6R7*KZ
        db: azrsqluat01-db-hr-discovery
